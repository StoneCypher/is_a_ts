{
  "name": "is_a_ts",
  "version": "0.14.1",
  "description": "Typescript implementation of is_a/2 dynamic typeguard by Krumpet, productionalized by StoneCypher",
  "main": "dist/index.cjs.min.js",
  "module": "dist/index.esm.min.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "version": "node ./src/build_js/write_version_file.js",
    "typescript": "tsc --build tsconfig.json",
    "terser_cjs": "terser --comments false -c passes=2 -m --sequences 40 build/rollup/index.cjs.js -o build/terser/index.cjs.min.js",
    "terser_esm": "terser --comments false -c passes=2 --sequences 40 build/rollup/index.esm.js -o build/terser/index.esm.min.js",
    "terser": "npm run terser_cjs && npm run terser_esm",
    "rollup": "rollup -c",
    "docs": "typedoc --out docs/docs src/ts && cp src/html/.nojekyll docs && cp src/html/.nojekyll docs/docs && cp src/html/.nojekyll docs/docs/assets && cp src/html/.nojekyll docs/docs/modules && cp src/html/.nojekyll docs/docs/classes",
    "clean": "rimraf -f docs && mkdir docs && rimraf -f build && mkdir build && cd build && mkdir typescript && mkdir rollup && mkdir terser && cd ../src/ts && rimraf -f generated_code && mkdir generated_code && cd ../..",
    "dist": "rimraf -f dist && mkdir dist && cp ./build/terser/index.cjs.min.js ./dist && cp ./build/terser/index.esm.min.js ./dist && cp ./build/typescript/index.d.ts ./dist",
    "site": "cp -r ./src/html/* ./docs && cp ./src/html/.nojekyll ./docs",
    "changelog": "auto-changelog --hide-credit --commit-limit false --unreleased",
    "build": "npm run test && npm run clean && npm run version && npm run typescript && npm run rollup && npm run terser && npm run dist && npm run docs && npm run site && npm run changelog",
    "test": "rimraf -f coverage && jest --color --coverage --verbose"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/StoneCypher/is_a_ts.git"
  },
  "keywords": [
    "is_a",
    "type",
    "guard",
    "typescript",
    "ts",
    "krumpet",
    "stonecypher"
  ],
  "author": "Krumpet <ran.lottem@mail.huji.ac.il>",
  "contributors": [
    "John Haugeland <stonecypher@gmail.com>"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/StoneCypher/is_a_ts/issues"
  },
  "homepage": "https://github.com/StoneCypher/is_a_ts#readme",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^19.0.1",
    "@rollup/plugin-node-resolve": "^13.0.3",
    "@types/jest": "^26.0.24",
    "auto-changelog": "^2.3.0",
    "jest": "^27.0.6",
    "rimraf": "^3.0.2",
    "rollup": "^2.53.3",
    "terser": "^5.7.1",
    "ts-jest": "^27.0.4",
    "typedoc": "^0.21.4",
    "typescript": "^4.3.5"
  }
}
